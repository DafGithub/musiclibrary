{% extends 'base.html.twig' %}

{% block title %}MuLib{% endblock %}

{% block body %}
    <div class="text-center mt-5">
        {% if app.user%}
            <h5>Welcome {{ app.user.username }} </h5>
        {% endif %}
    </div>
    <div class="container">
        <h2 class="mb-5 mt90 text-center">The latest songs</h2>
        {{ include('song/_ws.html.twig') }}
    </div>
{% endblock %}


